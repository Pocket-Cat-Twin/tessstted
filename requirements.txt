# Core dependencies for game monitoring system
pyautogui>=0.9.54
opencv-python>=4.8.0.76
pytesseract>=0.3.10
Pillow>=10.0.1
numpy>=1.24.3
keyboard>=0.13.5
PyYAML>=6.0.1

# Performance and utilities
psutil>=5.9.0

# GUI (tkinter is built-in with Python)
# Database (sqlite3 is built-in with Python)
# Threading, queue, time, logging are built-in